<h1 class="header-all-books grad">All Books</h1>

<div class="display-horizontal">
    {{#books}}
    <div class="container-book">
        <div class="container-title">
            <a class="" href="#/books/{{_id}}"><h4 class="container-book-title">{{{title}}}</h4></a>
            <p class="container-book-author">Author: {{author}}</p>
        </div>
        <div>
            <a href="#/books/{{_id}}"><img class="img-fluid img-size img-border" class="tooltip" data-tooltip-content="#tooltip_content" src="{{coverUrl}}"/></a>
            {{!--<div class="tooltip_templates">
                <span id="tooltip_content">
                    <img src="{{coverUrl}}"/><strong>{{title}}</strong>
                </span>
            </div>--}}
        </div>
        {{!--<div> 
            <h4>Pages: {{pages}}</h4>
        </div>--}}
       
    </div>
    {{/books}}
</div>
<div class="pages">
    {{!--<div class="previous">
        <a href="#/books?page={{prevPage}}&size=10" class="btn btn-info {{#if firstPage}}disabled{{/if}}">Prev</a>
    </div>--}}

    <ul class="pagination">
        {{!--<li class="page-item"><a class="page-link" href="#">Previous</a></li>--}}
        {{#pages}}
            <li class="page-item"><a class="page-link" href="{{link}}">{{number}}</a></li>
        {{/pages}}
        {{!--<li class="page-item"><a class="page-link" href="#">2</a></li>
        <li class="page-item"><a class="page-link" href="#">3</a></li>--}}
        {{!--<li class="page-item"><a class="page-link" href="#">Next</a></li>--}}
  </ul>

    {{!--{{#each pages}}
        <a href="{{link}}">{{number}}</a>
    {{/each}}--}}

    {{!--<div class="next">
        <a href="#/books?page={{nextPage}}&size=10" class="btn btn-info {{#if lastPage}}disabled{{/if}}">Next</a>
    </div>--}}
</div>